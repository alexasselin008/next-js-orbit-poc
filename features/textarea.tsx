import { Button, Content, Counter, Div, Heading, Inline, Item, Message, Popover, PopoverTrigger, Select, Text, TextArea, TextLink } from "@orbit-ui/components";

export function OrbitTextArea() {
  return(
    <>
    <Heading>TextArea</Heading>
<TextArea fluid placeholder="Why should you go to space?" />
    </>
  )
}